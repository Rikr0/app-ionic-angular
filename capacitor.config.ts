import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ricardo.app',
  appName: 'residentia-app',
  webDir: 'dist/residentia-app',
  bundledWebRuntime: false
};

export default config;
